name: node-setup
on:
  push:
    branch: node

jobs:
    setup:
       runs-on: ubuntu-latest
       steps:
           - uses: action/checkout@v3
           - uses: actions/setup-node@v3
             with:
                 node-version: '14'
           - run: install -g bats
           - run: bats -v      
